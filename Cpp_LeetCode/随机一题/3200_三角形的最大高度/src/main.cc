#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

/**
 * @brief 3200_三角形的最大高度
 * https://leetcode.cn/problems/maximum-height-of-a-triangle/
 */

int maxHeightOfTriangle(int red, int blue) {}

int main() {
  int redNum, blueNum;
  std::cin >> redNum >> blueNum;
  std::cout << maxHeightOfTriangle(redNum, blueNum) << std::endl;
  return 0;
}